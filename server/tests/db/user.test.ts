import * as dotenv from "dotenv";
dotenv.config();

import { createUser } from "../../services/db";

describe("Testing user schema", () => {
  test("Testing create user", () => {});
  test("Testing create user with insufficient data", () => {});
});
